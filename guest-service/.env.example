# Server
PORT=3004
NODE_ENV=development

# PostgreSQL
DB_HOST=postgres-guest
DB_PORT=5432
DB_NAME=guest_db
DB_USER=guest_user
DB_PASSWORD=guest_password

# RabbitMQ
RABBITMQ_URL=amqp://guest:guest@rabbitmq:5672
RABBITMQ_EXCHANGE=hotel_events

# JWT (verify only — same secret as auth-service)
JWT_SECRET=change_me_in_production_min_32_chars

# Internal token (shared across services)
INTERNAL_TOKEN=super_secret_internal_token

# Audit service (fire-and-forget HTTP)
AUDIT_SERVICE_URL=http://audit-service:3006
